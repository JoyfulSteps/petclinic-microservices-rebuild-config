server:
  port: 8082
spring:
  application:
    name: customers-service
  datasource:
    url: jdbc:hsqldb:mem:customersdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/