server:
  port: 8083

spring:
  application:
    name: vets-service
  datasource:
    url: jdbc:hsqldb:mem:vetsdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:18761/eureka/