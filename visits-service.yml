server:
  port: 18084
spring:
  application:
    name: visits-service
  datasource:
    url: jdbc:hsqldb:mem:ownersdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  kafka:
    producer:
      bootstrap-servers: host.docker.internal:19092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer


eureka:
  client:
    serviceUrl:
      defaultZone: http://host.docker.internal:18761/eureka/